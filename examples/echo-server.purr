; +----------------------+
; | Echo Server in Purr! |
; +----------------------+
; Building:
;   python -m skiio compile -i echo-server.purr -o echo-server
; Running:
;   python -m skiio run -i echo-server.ski

; Z Combinator
#Zcom
[f:[g:f(g(g))]([g:f([y:g(g)(y)])])]
~

; Main program
!
Zcom(
    [f:
        ATOM_OUT(ATOM_IN(ATOM_I))(f(ATOM_I)) ; loop indefinitely
    ]
)
~